//There're 2 ways to do test. First in a different folder, like the last section or like we'll do in this section, I'll create a test file near to the main file
describe("App", () => {
  test("Should be true", () => {
    expect(true).toBe(true);
  });
});
